# data/local/（临时专项回归清单）

`eval:fullchain` 会在运行时读取 `data/local/manifest.json`。

注意：本目录引用的样本路径指向 `.tmp/task-uploads/`，该目录属于临时产物区，可能会因后续清理操作而丢失；如果清理后脚本日志里出现 `skip manifest (missing)` 或 `skip sample ... missing source ...`，请先重新导入对应 PDF 或更新 manifest 路径。

