# export-agent Docs

This is the Markdown authority entry for the project.

## Authority

- Accepted current state, evidence, ADRs, requirements, and memory live under
  `docs/`.
- Platform output under `.ai-control/` is derived, runtime, candidate, or cache
  state only.

## Navigation

- Current entry: `docs/resume-brief.md`.
- Delivery, task routing, and skill guardrails: `docs/delivery/`.
- Initiatives, task slices, handoffs, and accepted evidence:
  `docs/initiatives/`.
- Reusable role cards and owner views: `docs/roles/`.
- Accepted reusable memory: `docs/memory/`.
- Authority boundary: `docs/standards/platform-boundary.md`.

Create product, architecture, or service-local folders only when the project
needs those authority surfaces.
